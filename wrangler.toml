name = "arcalab-xyz"
compatibility_date = "2025-11-15"

# Output directory for Next.js static export
# Next.js outputs static files to 'out' directory when using output: 'export'
pages_build_output_dir = "out"

# Node.js compatibility (not needed for static export, but keeping for future)
compatibility_flags = ["nodejs_compat"]
