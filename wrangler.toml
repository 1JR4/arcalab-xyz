name = "arcalab-xyz"
compatibility_date = "2025-11-15"

# For Next.js with App Router (not static export)
# Cloudflare Pages will handle the build automatically via GitHub integration

# This file is mainly for local development and configuration
compatibility_flags = ["nodejs_compat"]

# Pages configuration
# Build output will be handled by Cloudflare's Next.js integration
pages_build_output_dir = ".next"
